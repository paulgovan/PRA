# Fetch Available Ollama Models

Queries the local Ollama API for available models. Falls back to a
default list if the API is unreachable.

## Usage

``` r
get_ollama_models()
```

## Value

A character vector of model names.
