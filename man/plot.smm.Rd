% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smm.R
\name{plot.smm}
\alias{plot.smm}
\title{Plot Second Moment Method results.}
\usage{
\method{plot}{smm}(x, main = NULL, col = NULL, xlab = NULL, ...)
}
\arguments{
\item{x}{An object of class \code{"smm"}.}

\item{main}{Optional plot title. If \code{NULL}, a default title is generated.}

\item{col}{Fill color under the density curve. If \code{NULL}, uses the package
palette.}

\item{xlab}{Optional x-axis label.}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Displays the normal density implied by the propagated mean and standard
deviation, with the P50 and P95 percentiles marked.
}
\details{
The Second Moment Method constrains only two moments, so this curve is the
maximum-entropy distribution consistent with them rather than an estimate of
the true distribution's shape. The left tail is an artifact when
\code{total_mean} is less than roughly three standard deviations, since costs and
durations are non-negative and the normal is not. Use \code{\link[=plot.mcs]{plot.mcs()}} on a
\code{\link[=mcs]{mcs()}} result when the shape matters.
}
\examples{
plot(smm(c(10, 15, 20), c(4, 9, 16)))
}
