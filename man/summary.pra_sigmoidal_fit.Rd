% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigmoidal.R
\name{summary.pra_sigmoidal_fit}
\alias{summary.pra_sigmoidal_fit}
\title{Summarize a sigmoidal model fit.}
\usage{
\method{summary}{pra_sigmoidal_fit}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"pra_sigmoidal_fit"} returned by
\code{\link[=fit_sigmoidal]{fit_sigmoidal()}}.}

\item{...}{Additional arguments passed to \code{\link[stats:summary.nls]{stats::summary.nls()}}.}
}
\value{
An object of class \code{c("summary.pra_sigmoidal_fit", "summary.nls")}:
the standard \code{summary.nls} object with two additional components,
\code{model_type} (the sigmoidal family that was fitted) and \code{asymptote} (the
fitted upper bound, \code{K} for Pearl and Logistic models and \code{A} for
Gompertz). \code{model_type} is \code{NA} when the object did not come from
\code{\link[=fit_sigmoidal]{fit_sigmoidal()}}.
}
\description{
Extends the standard nonlinear least-squares summary with the sigmoidal model
type and the interpreted shape parameters.
}
\examples{
data <- data.frame(time = 1:10, completion = c(
  5, 15, 40, 60, 70, 75, 80, 85, 90, 95
))
fit <- fit_sigmoidal(data, "time", "completion", "logistic")
summary(fit)
}
