% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcs.R
\name{plot.mcs}
\alias{plot.mcs}
\title{Plot Monte Carlo Simulation results.}
\usage{
\method{plot}{mcs}(
  x,
  main = NULL,
  col = NULL,
  breaks = 50,
  normal_fit = TRUE,
  xlab = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"mcs"}.}

\item{main}{Optional plot title. If \code{NULL}, a default title is generated.}

\item{col}{Fill color for the histogram bars. If \code{NULL}, uses the package
palette.}

\item{breaks}{Number of histogram breaks, passed to \code{\link[graphics:hist]{graphics::hist()}}.}

\item{normal_fit}{Logical. Overlay the normal density implied by the
simulated mean and standard deviation. Skipped automatically when the
standard deviation is zero or non-finite.}

\item{xlab}{Optional x-axis label.}

\item{...}{Additional arguments passed to \code{\link[graphics:hist]{graphics::hist()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Displays the simulated total distribution as a histogram, optionally overlaid
with the moment-matched normal density, and marks the P50 and P95
percentiles.
}
\examples{
task_dists <- list(
  list(type = "normal", mean = 10, sd = 2),
  list(type = "uniform", min = 8, max = 12)
)
plot(mcs(1000, task_dists))
}
