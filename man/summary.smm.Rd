% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smm.R
\name{summary.smm}
\alias{summary.smm}
\title{Summarize Second Moment Method results.}
\usage{
\method{summary}{smm}(object, conf_level = 0.95, ...)
}
\arguments{
\item{object}{An object of class \code{"smm"}.}

\item{conf_level}{Unused placeholder retained for symmetry; percentiles are
fixed at P5/P50/P95.}

\item{...}{Additional arguments (not used).}
}
\value{
An object of class \code{"summary.smm"}, a list with components:
\describe{
\item{total_mean, total_variance, total_sd}{Propagated moments. Note the
canonical names, which match those used by \code{\link[=summary.mcs]{summary.mcs()}}; the \code{smm}
object itself carries \code{total_var} and \code{total_std}.}
\item{cv}{Coefficient of variation. \code{NA} when the mean is zero.}
\item{percentiles}{Named numeric vector of the P5, P50 and P95
percentiles implied by the normal approximation.}
}
}
\description{
Summarizes the propagated moments and reports the percentiles implied by a
normal approximation.
}
\details{
The Second Moment Method constrains only the first two moments of the total,
so the percentiles reported here are those of the normal distribution with
that mean and variance. This is the maximum-entropy distribution consistent
with what SMM computes, but it is an approximation: durations and costs are
non-negative while the normal is not, so the lower percentiles are unreliable
when \code{total_mean} is less than roughly three standard deviations. Use
\code{\link[=mcs]{mcs()}} when the shape of the distribution matters.
}
\examples{
result <- smm(c(10, 15, 20), c(4, 9, 16))
summary(result)
}
