% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigmoidal.R
\name{plot.pra_sigmoidal_fit}
\alias{plot.pra_sigmoidal_fit}
\title{Plot a sigmoidal model fit.}
\usage{
\method{plot}{pra_sigmoidal_fit}(
  x,
  main = NULL,
  col = "red",
  conf_level = NULL,
  n_points = 100,
  xlab = NULL,
  ylab = NULL,
  ci_col = "lightblue",
  pch = 16,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"pra_sigmoidal_fit"} returned by
\code{\link[=fit_sigmoidal]{fit_sigmoidal()}}.}

\item{main}{Optional plot title.}

\item{col}{Color of the fitted curve.}

\item{conf_level}{Confidence level for the band, for example \code{0.95}. If
\code{NULL} (default), no band is drawn.}

\item{n_points}{Number of points used to draw the fitted curve.}

\item{xlab, ylab}{Optional axis labels. Default to the fitted column names.}

\item{ci_col}{Fill color for the confidence band.}

\item{pch}{Plotting symbol for the observed data.}

\item{...}{Additional arguments passed to \code{\link[=plot_sigmoidal]{plot_sigmoidal()}}.}
}
\value{
Invisibly returns \code{x}.
}
\description{
Plots the observed data with the fitted sigmoidal curve and, optionally, a
confidence band. The data, column names and model type are recovered from the
fitted object, so no further arguments are required.
}
\examples{
data <- data.frame(time = 1:10, completion = c(
  5, 15, 40, 60, 70, 75, 80, 85, 90, 95
))
fit <- fit_sigmoidal(data, "time", "completion", "logistic")
plot(fit)
plot(fit, conf_level = 0.95)
}
\seealso{
\code{\link[=plot_sigmoidal]{plot_sigmoidal()}}, which takes the data and model type explicitly.
}
