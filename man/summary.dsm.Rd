% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsm.R
\name{summary.dsm}
\alias{summary.dsm}
\title{Summarize a DSM object.}
\usage{
\method{summary}{dsm}(object, n_top = 5, ...)
}
\arguments{
\item{object}{A \code{dsm} object returned by \code{\link[=parent_dsm]{parent_dsm()}} or \code{\link[=grandparent_dsm]{grandparent_dsm()}}.}

\item{n_top}{Number of most strongly coupled task pairs to report. Defaults
to 5.}

\item{...}{Additional arguments (not used).}
}
\value{
An object of class \code{"summary.dsm"}, a list with components:
\describe{
\item{type}{Either \code{"parent"} or \code{"grandparent"}.}
\item{n_tasks, n_resources, n_risks}{Dimensions of the underlying
incidence matrices. \code{n_risks} is \code{NULL} for a parent DSM.}
\item{density}{Proportion of off-diagonal cells that are non-zero.}
\item{total_coupling}{Sum of the upper triangle, the total number of
shared dependencies across all task pairs.}
\item{own}{Named numeric vector: the diagonal, each task's own count of
resources (parent) or risks (grandparent).}
\item{degree}{Named numeric vector: each task's total coupling to the
other tasks.}
\item{top_pairs}{Data frame of the \code{n_top} most strongly coupled task
pairs, with columns \code{task_a}, \code{task_b} and \code{shared}.}
}
}
\description{
Summarizes the coupling structure of a Design Structure Matrix: how densely
the tasks are connected, how much coupling each task carries, and which task
pairs share the most resources (parent) or risks (grandparent).
}
\examples{
# A project with 3 tasks and 2 shared resources.
S <- matrix(c(
  1, 1, 0,
  0, 1, 1
), nrow = 2, byrow = TRUE)
colnames(S) <- c("Design", "Build", "Test")
p <- parent_dsm(S)
summary(p)
}
